---
layout: base
pageTemplate: tours
pagination:
  alias: tour
  data: tours
  size: 1
permalink: "{{ tour.slug | slug }}/"
---
{# 
eleventyComputed:
title: "{{ project.seo.title }}"
description: "{{ project.seo.description }}"
image: "{{ project.seo.seo_image }}" #}

{{ tour | dump | safe}}
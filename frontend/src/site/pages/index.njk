---
layout: base
pageTemplate: index
---
{# ---
layout: base
pageTemplate: index
eleventyComputed:
  title: "{{ copies.seo.title }}"
  description: "{{ copies.seo.description }}"
--- #}

{% from 'buttons/button.njk' import button as button %}

<div class="center__wrapper">

    {# TODO imatge del gestor #}
    <img src="https://picsum.photos/300/200?grayscale" alt="{{ landing.title }}"> 
    <h1>{{ landing.title }}</h1>
    <p>{{ landing.version }}</p>

</div>

<div class="buttons__wrapper">

  {{ button(landing.leftButton) }}
  {{ button(landing.rightButton) }}
  
</div>

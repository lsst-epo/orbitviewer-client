import { onChange } from "../pagination/History";
import { Page } from "./Page";


export class Landing extends Page {
	addEventListeners(): void {
		
		// const btn1 = this.dom.querySelector('[data-button="landing-button-left"]');
		// const btn2 = this.dom.querySelector('[data-button="landing-button-right"]');

		// btn1.addEventListener('click', (e) => {
		// 	e.preventDefault();
		// 	onChange('/customize-orbits');
		// })
		// btn2.addEventListener('click', (e) => {
		// 	e.preventDefault();
		// 	onChange('/guided-experiences');
		// })
	}
}
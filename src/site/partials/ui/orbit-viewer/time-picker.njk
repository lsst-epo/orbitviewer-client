{% from 'ui/panel-times.njk' import panelTimes as panelTimes %}

<div class="time-picker">
	<button data-panel-button="time-picker" class="orbit-button">
		{% include "ui/icons/time-picker.njk" %}
	</button>
</div>

<div class="panel panel-bottom-center" data-panel="time-picker">

	<div class="panel-top">
		<div></div>
		{{ panelTimes('time-picker', 'small') }}
	</div>

	<div class="panel-section timer-picker-description">
		<p>{{ generic[lang]['special-strings']['date-explanation'] }}</p>
	</div>

	<div class="panel-section time-picker-input-wrapper">

		<div class="time-picker-input">
			
			<p>{{ generic[lang].strings.past }}</p>		
			<p>{{ generic[lang].strings.future }}</p>
			<input type="range" min="-1" max="1" step="0.00001" class="custom-input" data-timer="1">
			{% include "ui/icons/time-picker-chevs.njk" %}

		</div>

		<div class="time-picker-icon-wrapper">
			<div class="time-picker-icon">
				<div class="bg"></div>
				{% include "ui/icons/time-picker.njk" %}
			</div>

		</div>

	</div>

	<div class="panel-section time-picker-details">
		<p>{{ generic[lang]['special-strings']['current-date'] }} <span>00/00/0000 - 00:00:00 am</span></p>

		<div>
			<button class="ui__button-small" data-timer="reset">{{ generic[lang].strings.reset }}</button>
			<button class="ui__button-small" data-timer="edit">{{ generic[lang].strings.edit }}</button>
		</div>
	</div>

	<div class="sub-panel">
		<div class="input-wrapper date-picker custom-input" data-date="1">

				<input type="date">

				<div class="date-item">
					<p>{{ generic[lang].strings.day }}</p>
					<h4>DD</h4>
				</div>
				<div class="date-item">
					<p>{{ generic[lang].strings.month }}</p>
					<h4>MM</h4>
				</div>
				<div class="date-item">
					<p>{{ generic[lang].strings.year }}</p>
					<h4>{% if lang == 'en' %}YY{% else %}AA{% endif %}</h4>
				</div>

		</div>

		<div class="buttons-wrapper">

			<button data-button="apply-date" class="ui__button grey-button">{{ generic[lang]['special-strings']['go-date'] }}</button>

			<button data-button="close-edit" class="ui__button ghost-button">{{ generic[lang].strings.cancel }}</button>

		</div>
		
	</div>

</div>


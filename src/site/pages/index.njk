---
layout: base
pageTemplate: index
---
{# ---
layout: base
pageTemplate: index
eleventyComputed:
  title: "{{ copies.seo.title }}"
  description: "{{ copies.seo.description }}"
--- #}

<div class="center__wrapper">

    {# TODO imatge del gestor #}
    <img src="https://picsum.photos/300/200" alt="{{ landing.title }}"> 
    <h1>{{ landing.title }}</h1>
    <p>{{ landing.version }}</p>

</div>

<div class="buttons__wrapper">

  <button data-button="landing-button-left" class="ui__button">
    <p>{{ landing.leftButton }}</p>
  </button>

  <button data-button="landing-button-right" class="ui__button">
    <p>{{ landing.rightButton }}</p>
  </button>

</div>
